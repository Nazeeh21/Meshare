query Bookmarks {
  bookmarks {
    githubId
    questionId
    question {
      id
      title
      description
      imageUrls
      tags
      voteStatus
      githubId
      createdAt
      creator {
        avatarUrl
        name
        githubId
      }
    }
    createdAt
  }
}
