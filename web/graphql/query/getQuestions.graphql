query Questions($limit: Int!, $cursor: String) {
  questions(cursor: $cursor, limit: $limit) {
    hasMore
    questions {
      id
      title
      text
      html
      imageUrls
      tags
      points
      bookmarkStatus
      voteStatus
      bountyAmount
      githubId
      createdAt
      creator {
        githubId
        avatarUrl
        name
      }
    }
  }
}
