mutation CreateQuestion(
  $title: String!
  $text: String!
  $html: String!
  $tags: [String!]!
  $imageUrls: [String!]!
) {
  createQuestion(
    input: {
      title: $title
      text: $text
      html: $html
      tags: $tags
      imageUrls: $imageUrls
    }
  ) {
    id
    title
    html
    text
    tags
    imageUrls
    voteStatus
    creator {
      name
    }
  }
}
